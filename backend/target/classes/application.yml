server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aiflow?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: fgld@223v
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms

jwt:
  secret: my-secret-key-for-jwt-token-generation-ai-flow-platform
  expiration: 86400000

# Flowable 流程引擎（暂时禁用）
# flowable:
#   database-schema-update: false
#   async-executor-activate: false
