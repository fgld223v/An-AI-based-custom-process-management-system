<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://aiflow.com/process">

    <!-- 简单审批流程示例：开始 -> 审批 -> 结束 -->
    <process id="simple_approval" name="简单审批流程" isExecutable="true">
        
        <!-- 开始节点 -->
        <startEvent id="startEvent" name="开始"/>
        
        <!-- 用户任务：审批 -->
        <userTask id="approveTask" name="审批" flowable:assignee="${assignee}"/>
        
        <!-- 结束节点 -->
        <endEvent id="endEvent" name="结束"/>
        
        <!-- 连线 -->
        <sequenceFlow id="flow1" sourceRef="startEvent" targetRef="approveTask"/>
        <sequenceFlow id="flow2" sourceRef="approveTask" targetRef="endEvent"/>
        
    </process>

</definitions>
