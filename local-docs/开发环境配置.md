# 开发环境配置（不上传 GitHub）

## 必需环境变量

### DEEPSEEK_API_KEY
DeepSeek API 密钥，用于 AI 模块调用 LLM。

```powershell
# Windows PowerShell 设置（以管理员身份运行，或当前用户）：
[Environment]::SetEnvironmentVariable('DEEPSEEK_API_KEY', 'sk-441b005825794b5fb090012a7a46e87f', 'User')
```

设置后需重启 IDE / 终端才能生效。

## 数据库

- MySQL 8.0
- 数据库名：`ai_workflow_mvp`
- 用户：`root`
- 密码：`Xia20041004`
